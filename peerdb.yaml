catalog:
  postgres:
    host: postgres
    port: 5432
    user: app
    password: supersecret123
    database: app

  clickhouse:
    host: clickhouse
    port: 9000
    user: default
    password: ""
    database: default

mirrors:
  realtime_mirror:
    source: postgres
    destination: clickhouse
    tables:
      - public.*                # replica TODAS as tabelas (ou liste só as que quiser)
    initial_load: true
    realtime: true
    resync_on_schema_change: true
